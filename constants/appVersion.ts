/**
 * App version — single source of truth.
 * Referenced by: profile page, FAQ footer, admin changelog.
 * Update this value when releasing a new version.
 */
export const APP_VERSION = '1.58.0';
export const APP_NAME = 'Ultimate Petanque';
export const APP_VERSION_DISPLAY = `${APP_NAME} v${APP_VERSION}`;
