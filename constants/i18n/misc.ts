/**
 * Placeholder — all sections have been migrated to dedicated topic files.
 * This file is kept for reference. The barrel (index.ts) imports directly
 * from each topic file and falls back to the original i18n.ts for any
 * sections not yet extracted (cadrage, notation, faq, challengeNames,
 * precisionWorkshops are still served from the original file via fallback).
 */
